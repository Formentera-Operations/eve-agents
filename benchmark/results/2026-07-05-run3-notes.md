# 2026-07-05 — Run 3: post-merge regression check (PR #4)

Confirmation run on merged main after per-class analyst model overrides
landed (logs analyst → claude-haiku-4.5; all else on the Sonnet 5 default).

- 25/25 completed, 25/25 correct with verifiable citations on manual
  grading (two automated-grader flags were string-format artifacts, same
  pattern as run 2 — q05 citation split across lines, q13 rounded delta
  with both exact values present).
- Delegation eval passed 4/4 gates against the same build — the
  eve → delegate_analysis → analyst-SubAgent path healthy under the new
  model config.
- Expected no-op by construction: no benchmark question routes log-class
  documents through delegate_analysis, so the Haiku change had no scored
  surface. Now confirmed rather than assumed.
